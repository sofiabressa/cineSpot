import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
  header: {
    backgroundColor: '#333',
    padding: 20,
    textAlign: 'center',
    color: 'white',
    fontSize: 18,
  },
});

export default globalStyles;
